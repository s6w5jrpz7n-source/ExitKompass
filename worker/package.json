{
  "name": "exitkompass-coach",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for the ExitKompass AI coach (Gemini Flash).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
